replicas:
  min: 1
  max: 4
  resource:
    cpu:
      utilization: 90

image:
  repository: docker.pkg.github.com/quantu-app/quantu-app/quantu-app-ui
  pullPolicy: Always
  hash: sha256:2af310fac13872efeed20df46547f19616fecab261e1dd0a23bc63c5b75ad30f

env:
  NODE_ENV: production

ingress:
  enabled: true
  annotations:
    kubernetes.io/tls-acme: 'true'
    certmanager.k8s.io/cluster-issuer: letsencrypt
    nginx.ingress.kubernetes.io/secure-backends: 'true'
  hosts:
    - host: main.quantu.app
      paths:
        - /
  tls:
    - secretName: main-quantu-app-tls
      hosts:
        - main.quantu.app

resources:
  requests:
    cpu: 15m
    memory: 105M
  limits:
    cpu: 50m
    memory: 128M
